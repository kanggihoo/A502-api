[project]
name = "jira-api-test"
version = "0.1.0"
description = "SSAFY Jira Cloud REST API v3 동작 검증 POC"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
